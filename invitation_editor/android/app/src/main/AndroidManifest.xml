<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      The app is fully offline: there is no INTERNET permission on
      purpose, per the "100% offline" requirement in
      project-architecture.pdf, Section 3.

      RECORD_AUDIO is for the "Voice Type" feature (UI/UX spec,
      Screen 4). Speech recognition runs through the on-device
      recogniser.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="Invitation Editor"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!--
              Shows a themed launch screen (ivory background) before
              Flutter renders its first frame.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
      Required on Android 11+ so speech_to_text can see the device's
      speech recognition service and text-to-speech engine.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
</manifest>
