<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Same as debug: needed for profiling over the local network. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
